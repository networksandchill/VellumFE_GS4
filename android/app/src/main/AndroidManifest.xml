<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Sideload app: the Play Store restricts this permission, but a
         persistent game session is exactly its intended use case. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:label="VellumFE"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- vellum:// deep links prefill a login tab (never
                 auto-connect): lich?host=...&port=... (QR / tapped link on
                 Lich's connect-a-device panel) → Lich tab;
                 remote?host=...&port=...&token=... (.webinfo's app QR on a
                 desktop VellumFE) → Remote tab. singleTask above routes
                 these through onNewIntent when the app is already
                 running. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vellum" android:host="lich" />
                <data android:scheme="vellum" android:host="remote" />
            </intent-filter>
        </activity>

        <!-- specialUse (not dataSync): a persistent game connection has no
             6-hour budget; Android 15 caps dataSync services at 6h. -->
        <service
            android:name=".CoreService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent connection to a text-game server the user is actively playing" />
        </service>
    </application>
</manifest>
